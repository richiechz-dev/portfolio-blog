---
const { platform, username } = Astro.props;
---
<a class="mr-4 font-mono text-xs text-zinc-500 transition-colors hover:text-zinc-100 sm:text-sm" href={`https://www.${platform}.com/${username}`}>
    {platform}
</a>
